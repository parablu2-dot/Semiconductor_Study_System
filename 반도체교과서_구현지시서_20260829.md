# 반도체 Study System — Claude Code 구현 지시서

대상 리포: `parablu2-dot/Semiconductor_Study_System`
원류: 구글 드라이브 `반도체교과서/` 폴더 (md 8장 + 종합)
목표: 드라이브의 md를 GitHub에 올리고, 서버에서 계층 대시보드로 열람

---

## 1. 파이프라인

```
Google Drive/반도체교과서/*.md
        │  (수동 다운로드 또는 rclone)
        ▼
   content/*.md          ← 원문. 사람이 읽고 고치는 유일한 곳
        │  build.js
        ▼
   public/data.json      ← 대시보드가 읽는 트리
        │
        ▼
   public/index.html     ← 대시보드 (뼈대 제공됨)
```

**드라이브를 원류로 두는 게 맞습니다.** 다만 GitHub에 올린 뒤에는 어느 쪽이 정본인지 한쪽으로 정해야 합니다.
양쪽에서 고치기 시작하면 며칠 안에 어긋납니다. 권장: **드라이브가 초안·수집, GitHub이 정본.**
GitHub에 올린 뒤로는 드라이브 파일을 고치지 않습니다.

## 2. 리포 구조

```
Semiconductor_Study_System/
├─ content/                     # md 원문 (정본)
│  ├─ 01_재료.md
│  ├─ 02_소자.md
│  ├─ ... 08_패키징집적.md
│  └─ 00_종합.md
├─ figures/                     # 원리 그림 SVG. 파일명 = 노드 id
│  ├─ ch1.svg  ch1-q2.svg ...
├─ public/
│  ├─ index.html                # 대시보드
│  └─ data.json                 # build 산출물 (커밋함)
├─ build.js                     # md → data.json
└─ README.md
```

## 3. md 규약 (build.js가 이 규칙으로 파싱)

각 장 파일은 4문항 구조를 그대로 유지합니다. 헤딩 레벨만 지키면 됩니다.

```markdown
---
id: ch1
no: "01"
title: 재료
lede: Si·화합물, 유전체, 도전체, 기판
---

## 1. 현 상태
요약 문단...

### basis: <근거 제목>        ← 있으면 한 겹 더 깊은 카드가 됨
내용...

## 2. 한계
...
```

- `## N. <문항명>` → 소주제 노드 (id 자동: `ch1-q1`)
- `### basis: <제목>` → 그 아래 depth 3 노드
- 문항 제목은 고치지 않습니다. 구조 고정, 깊이만 아래로.

## 4. build.js가 할 일

1. `content/*.md` 읽어 frontmatter + 헤딩으로 트리 구성
2. 각 노드 첫 문단을 `summary`, 나머지를 `body`(marked로 HTML 변환)
3. `figures/<id>.svg` 가 있으면 `figure.svg`에 인라인으로 넣기
4. 계층 순서는 파일명 번호 순 — **재료 → 소자 → 공정·계측 → 회로·IP → 아키텍처 → 설계 → 시스템·SW → 패키징·집적**
5. `public/data.json` 출력

의존: `gray-matter`, `marked`. 그 이상 붙이지 않습니다.

## 5. index.html 수정점

제공된 뼈대는 DATA가 파일 안에 박혀 있습니다. 서버용으로는 한 줄만 바꿉니다.

```js
// const DATA = { ... }   →
const DATA = await fetch("./data.json").then(r => r.json());
```

`figure`는 노드에 있으면 자동으로 "원리 그림 보기" 버튼이 생깁니다. 없으면 버튼도 안 생깁니다.

## 6. 원리 그림 정책

**그림은 미리 만들어 커밋합니다.** 열람 시점에 생성하지 않습니다.
매번 생성하면 같은 개념이 접속할 때마다 다르게 그려져서 기억에 남지 않고, 서버가 모델을 붙들고 있어야 합니다.
Claude Code로 SVG를 만들어 `figures/<노드id>.svg`로 저장하고, 마음에 들 때까지 고친 뒤 커밋하는 흐름이 맞습니다.

그림 제작 프롬프트 기준:
- 한 장에 개념 하나. 여러 개를 한 그림에 넣지 않습니다.
- 수치·연도를 넣지 않습니다. 내용이 바뀌면 그림이 거짓말이 됩니다.
- 색은 대시보드 팔레트만: `#B5761D` `#6E3A24` `#2E5E5B` `#D6C6AE` `#F6EFE3`
- 텍스트는 한국어, 6단어 이내 라벨만.

## 6-b. 출처 정책 (공개 사이트이므로 필수)

### md 표기

각 노드 끝에 `**출처**` 목록을 둡니다. build.js가 이 블록을 `sources[]`로 파싱합니다.

```markdown
## 2. 한계
본문...

**출처**
- [제목](https://...) — 논문 · 학회/저널명 · 2025
- [제목](https://...) — 표준 · 기관명 · 2024
- [제목](https://...) — 기업자료 · 회사명 · 2026
```

`kind`는 **논문 / 표준 / 기업자료 / 보도 / 특허** 다섯 가지로 고정합니다.
종류를 고정해야 나중에 "이 장은 보도자료만으로 서 있다" 같은 판정이 가능합니다.

### 원칙

1. **딥 리서치가 준 출처를 그대로 옮기지 않습니다.** 링크를 열어 그 문장이 실제로 있는지 확인한 것만 싣습니다.
   확인 못 한 항목은 `sources`에서 빼고 본문 서술도 같이 뺍니다.
2. **원문 인용은 하지 않고 재서술합니다.** 공개 사이트라 인용 분량 문제가 실제로 발생합니다.
   불가피하면 한 문장 이내로 하고 출처를 붙입니다.
3. **1차 자료를 우선합니다.** 논문·표준 문서·기업 기술자료 > 애널리스트 요약 > 보도.
   같은 사실이면 위쪽으로 교체합니다.
4. **출처 없는 서술은 싣지 않습니다.** 대시보드 카드에 `출처 —`로 표시되므로 빈 곳이 바로 보입니다.
5. **접근일**을 함께 남깁니다. 링크는 끊깁니다.

### build.js 추가 요구

- `**출처**` 이하 리스트를 파싱해 `sources: [{t,u,kind,pub,date}]` 생성
- 링크 없는 항목(`u` 빈 값)도 허용하되 카운트에는 포함
- 전체 출처 수를 루트 노드 `stats`에 집계

## 7. 서버

정적 파일만 있으므로 GitHub Pages로 충분합니다.
사내 열람이 필요해지면 그때 사내망 정적 서버로 같은 `public/`을 올리면 됩니다.
**리포는 공개 여부를 먼저 정하세요.** 여기 올라가는 건 사외 일반론뿐이고, 사내 basis는 이 리포에 넣지 않습니다.

## 8. Claude Code 착수 프롬프트

```
이 리포에 다음을 만들어라.

1. content/ 에 있는 md 8개를 파싱해 public/data.json 을 만드는 build.js
   - frontmatter: id, no, title, lede
   - "## N. 제목" → 소주제 노드 (id: <장id>-q<N>)
   - "### basis: 제목" → 그 아래 자식 노드
   - 각 노드의 첫 문단은 summary 배열, 나머지는 body(HTML)
   - figures/<id>.svg 가 있으면 figure.svg 로 인라인
   - 의존은 gray-matter, marked 만 사용

2. public/index.html 의 DATA 를 data.json fetch 로 교체

3. npm script: "build": "node build.js"

4. content/ 가 비어 있어도 build 가 실패하지 않고 빈 트리를 내도록 할 것
```
